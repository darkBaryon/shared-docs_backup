# 小程序后续模块计划

## 背景

小程序 auth、房源搜索、公开详情、资料、收藏、足迹已经完成。后续非第一阶段模块按业务优先级单独推进。

## 待实现模块

- 看房计划。
- 通知中心。

## 约束

- 继续使用 miniapp 端侧应用服务，不复用 publish 或 admin service。
- 接口字段和路径先写入 [../../../api/miniapp-api.md](../../../api/miniapp-api.md)。
- 数据库字段、枚举、索引同步到 `schema/`。
- 对房源展示的读取继续以 HPD 展示层为准，不直接读 HMD。

## 下一步

1. 确认看房计划和通知中心的业务优先级。
2. 拆分第一批接口契约。
3. 更新 schema 和 flow。
4. 实现后更新 [../status.md](../status.md)。
