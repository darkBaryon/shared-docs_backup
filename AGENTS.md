# AGENTS

## 作用范围

本规则适用于 `shared-docs/` 目录下全部文件与子目录。

## 仓库定位

这是共享文档仓库，不承载实现代码。

## 契约优先级

1. API 路径、方法、请求响应：以 `api/openapi.yaml` 为准（唯一 API 契约源）。
2. 接口补充说明与映射：以 `api/` 下文档为准，并与 OpenAPI 保持同步。
3. 数据库结构与后端 CRUD 约定：以 `schema/` 为准。
4. 历史调整说明与同步记录：以 `changes/` 为准。

## 编辑规则

1. 输入类文档（需求、会议结论、阶段性确认）默认作为历史记录保留，不直接改写历史语义。
2. 规范类文档（API/Schema）是当前事实来源；接口变更必须同时更新：
   - `api/openapi.yaml`
   - `api/` 下对应接口说明文档
   - `schema/` 下对应数据库/CRUD 文档
3. 若“当前实现/最终策略”与输入类文档存在差异：
   - 在 `changes/` 下新增日期化说明文档（例如：`*-adjustments-YYYY-MM-DD.md`）。
   - 通过“新增说明”表达差异，不覆盖原始输入记录。
4. 文档尽量追加式更新，避免覆盖历史上下文。
5. 发生跨文件调整时，确保契约一致，避免“OpenAPI 与说明文档不一致”。

## 协作约定（前后端共享）

1. 本规范同时面向后端与前端.
2. 讨论接口是否可用、字段是否变更时，以 `api/openapi.yaml` 为统一判断依据。
3. 讨论“为什么和原需求不一致”时，以 `changes/*-adjustments-YYYY-MM-DD.md` 为统一解释依据。
4. 新增接口前，先补契约再联调；变更接口后，先更新契约再通知对端。

## 交付检查清单

- [ ] `api/openapi.yaml` 已同步接口变更
- [ ] `schema/` 已同步数据库结构、字段语义与 CRUD 约定
- [ ] 若存在口径调整，已新增一份 `changes/*-adjustments-YYYY-MM-DD.md`
- [ ] 输入类文档与规范类文档语义一致，或已有差异说明文档
