# AGENTS

## 仓库定位

这是共享契约仓库，不承载实现代码。

## 规范优先级

- API 路径、方法、请求响应以 `api/openapi.yaml` 为准。
- 字段语义与枚举以 `schemas/` 为准。
- 页面到接口调用流程以 `contracts/` 为准。

## 写作约束

- 文档保持简洁、权威、一致。
- 跨文件修改时必须同步，避免契约冲突。
- 不写业务实现细节，不写代码片段以外的实现方案。
